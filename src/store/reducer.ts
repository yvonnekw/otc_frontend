// reducer.ts
import { ActionTypes, SET_USER, SET_CALLS, SET_ERROR_MESSAGE } from './types';
/*
const initialState: State = {
    user: null,
    calls: [],
    errorMessage: '',
};

const rootReducer = (state = initialState, action: ActionTypes): State => {
    switch (action.type) {
        case SET_USER:
            return {
                ...state,
                user: action.payload,
            };
        case SET_CALLS:
            return {
                ...state,
                calls: action.payload,
            };
        case SET_ERROR_MESSAGE:
            return {
                ...state,
                errorMessage: action.payload,
            };
        default:
            return state;
    }
};

export default rootReducer;

*/


